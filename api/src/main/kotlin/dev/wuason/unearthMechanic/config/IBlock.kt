package dev.wuason.unearthMechanic.config

/**
 * Represents a generic block that can be interacted with or managed.
 *
 * Implementations of this interface define the behavior and properties of the block.
 * The interface is utilized to detect if an object is a block and to handle block-specific
 * interactions and events.
 */
interface IBlock {
}